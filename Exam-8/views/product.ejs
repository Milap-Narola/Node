<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f4f4f4;
        }
        .product {
            background: white;
            border: 1px solid #ddd;
            border-radius: 5px;
            padding: 10px;
            margin: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        .product-list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 20px;
        }
    </style>
</head>
<body>
    <h1>Product List</h1>

    <% products.forEach(product => { %>
        <div class='product'>
            <h2><%= product.title %></h2>
            <p>Price: $<%= product.price %></p>
            <% if (product.img) { %> 
                <img src="<%= product.img %>" alt="<%= product.title %>" style='width:100px; height:auto;'>
            <% } %> 
        </div>
    <% }) %>
</body>
</html>